# @prisma-kb/driver-adapter-utils

Runtime utilities and TypeScript types for the Prisma Kingbase driver adapter.
It reuses Prisma's driver-adapter implementation while adding the
`kingbase-mysql` and `kingbase-oracle` provider types.

This is an implementation dependency of `@prisma-kb/adapter-kb`; applications
should normally install the adapter package instead of this package directly.
